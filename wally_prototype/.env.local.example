# 複製成 .env.local 再填值。**.env.local 不要進 git**（已被 .gitignore 擋住）。
#
# 後端 API Gateway 的 Invoke URL。從 backend/.env.deploy 的 API_BASE 拿，
# 或 AWS Console → API Gateway → appeal-api-gw → Invoke URL。
#
# ⚠️⚠️ **結尾不要有任何符號** —— 不要斜線、不要空白、不要引號。
#
#    特別注意 macOS：在 zsh 裡 `cat .env.local` 之類的輸出如果沒有換行結尾，
#    終端機會顯示一個反白的「%」。**那是提示符號不是字元**，但複製時會被
#    一起帶進去，變成：
#
#        Failed to execute 'fetch': Failed to parse URL from https://...com%/cases
#
#    （2026-09-12 實際踩過。程式碼現在會自動清掉，但還是不要貼進去。）
#
# ⚠️ 改完這個檔案**一定要重啟 dev server**，Vite 只在啟動時讀環境變數。
#
# ⚠️ **不要把 AWS 金鑰放進這裡。** Vite 會把 VITE_ 開頭的變數編進 bundle
#    送到瀏覽器，按 F12 就看得到。

VITE_API_BASE=
